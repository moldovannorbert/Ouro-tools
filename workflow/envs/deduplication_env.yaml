channels:
  - conda-forge
  - bioconda
  - defaults

dependencies:
  - python>=3.8
  - biopython
  - tqdm
  - python-edlib  # Fast edit distance
  - pyspoa   # POA consensus generation
  - pysam  # For handling compressed files
